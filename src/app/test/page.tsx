import CircleProgress from '@/components/ui/CircleProgress';
import Glass from '@/components/ui/Glass';

const Page = async () => {
  return <CircleProgress className="size-20" value={50} strokeWidth={15} />;
};

export default Page;
